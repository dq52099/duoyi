# 手动回归清单（Task 25）

对齐 `.kiro/specs/app-alignment-overhaul/requirements.md`。在可用真机上按顺序走完以下 14 条，出现偏差在"状态"栏打叉 + 补截图到 Issue。

## 1. 启动 & 时区
- [ ] 冷启动后在"我的 → 关于"看到当前 IANA 时区信息（可选；至少日志里 `LocalTimezoneResolver.diagnostics` 为空或只有非致命回退）。
- [ ] 断网启动 → 所有 Tab 可用；App 底部不显示"同步失败"红色状态。

## 2. 推荐目标
- [ ] "目标"页右上 ✨ 按钮 → 打开推荐目标面板。
- [ ] 切换"健康 / 学习 / 运动 / 情感 / 推荐" 5 个分段，每个都有 ≥ 5 条条目。
- [ ] 点击任一推荐条目 → SnackBar "已添加到目标列表"；返回"目标"页能看到新条目。
- [ ] 新条目打开编辑页 → 8 个折叠模块（基础/重复/调度/跳节假日/专注/提醒/时长/次数）都可展开。

## 3. 任务交互
- [ ] 新建一个任务 → 详情页编辑标题 → 点 AppBar ✓ → SnackBar"已保存" + 详情页**不返回**。
- [ ] 继续改标题 → 按系统返回键 → 弹"放弃未保存的修改？" → 点"取消" → 仍在详情页。
- [ ] 再按返回 → 点"放弃" → 回列表，改动未落盘。
- [ ] 任务卡片显示：优先级色点、标签胶囊、下次提醒 HH:MM、目标时长 badge。
- [ ] 勾选完成 → 卡片**保留在今日列表**，显示"已完成"绿色徽章 + 删除线。
- [ ] 取消勾选 → 回到普通态，徽章消失。

## 4. 子任务 / 顺延
- [ ] 新建任务带 3 个子任务，`autoToggleByChildren = true`（默认）。
- [ ] 依次勾选 3 个子任务 → 父任务自动标为完成。
- [ ] 取消其中一个 → 父任务自动回到未完成。
- [ ] 新建任务设置昨日 dueDate → 下次启动或 resume 时 → `dueDate` 自动顺延到今日同一时刻，`postponeHistory` 增加一条。

## 5. 通知 vs 闹钟
- [ ] 偏好设置里的「通知健康检查」卡片显示：系统通知权限、精准闹钟权限、通知渠道、测试通知、已调度提醒。
- [ ] HyperOS / MIUI 设备显示自启动、后台与电池、锁屏与横幅、渠道声音四类人工检查项；这些检查项不出现重复的「去设置」按钮，只保留一个疑难设置入口。
- [ ] 从系统设置返回后，卡片状态刷新，提醒详情里的健康提示同步更新。
- [ ] 任务详情页：提醒开关 = ON，kind = **推送**，时间 = 1 分钟后 → 到点系统通知中心出现，不全屏。
- [ ] 切换 kind = **闹钟**，时间 = 1 分钟后 → 到点**全屏 intent 弹出 + 震动**。
- [ ] Android 12+：第一次选闹钟弹"请开启精准闹钟权限"引导（`AlarmPermissionDeniedException` 被捕获时）。

## 6. 专注模式白噪音
- [ ] 番茄钟选"雨声" → 启动 → 扬声器真实播放循环雨声。
- [ ] 暂停 → 500ms 内静音（fadeOut 300ms + tail）。
- [ ] 锁屏并等 30s → 解锁后仍在播放（Android `foregroundServiceType=mediaPlayback`）。
- [ ] 切换到"无白噪音" → 立即停播。

## 7. Today 页
- [ ] 有待办时，"查看"按钮 → 进入 TodoScreen。
- [ ] 无待办 + 点某个已删除条目（模拟）→ 跳到 EmptyState 兜底页而非黑屏。
- [ ] 四个指标卡（待办 / 习惯 / 专注 / 日记）点击都能跳到对应 Tab。
- [ ] 四个指标卡与全局卡片风格一致，数字/单位字号紧凑，移动端不出现大字号挤压或明显断层。
- [ ] 今日待办、今日课程标题不拼接大段数量；数量和已完成数显示在副标题里，视觉层级低于标题。

## 7a. 我的页
- [ ] 四个指标卡（待办完成 / 连续打卡 / 本周专注 / 效率评分）与 Today 指标卡风格一致，数字/单位字号紧凑。
- [ ] 功能入口按计划执行、日程资料、记录回顾、智能工具、设置安全、协作同步、消息支持分组展示。
- [ ] 目标管理、课程表、纪念日、生日、倒数日、万年历、黄历、备份、恢复数据、功能建议、问题反馈、许愿池都保留独立入口，且每个入口有外边框。

## 8. 日历
- [ ] 月视图点击任一天 → 日期高亮 + 下方列表刷新为当日事项。
- [ ] 切换月份，选中的日期视觉跟随。
- [ ] 当日无任何事项 → 日代理区域显示"空态"。

## 8a. 第三方日历 OAuth
- [ ] 我的 → 扩展集成 → 日历订阅：看到“Google / Outlook OAuth 日历”卡片。
- [ ] 填入 Google 或 Outlook 的 Client ID、Redirect URI、Calendar ID → 授权链接自动生成，复制和外部打开可用。
- [ ] 点击“打开”进入授权页前会保存 PKCE 草稿；浏览器回到 `duoyi://oauth/calendar?code=...` 后，App 自动打开扩展集成页并预填回调 URL。
- [ ] 粘贴授权码或使用自动预填的回调 URL → 保存后账号出现在列表，点击刷新后日历能看到远端事件。
- [ ] 外部 ICS / Google / Outlook 事件在日历详情中只展示，不出现“改期 / 调整开始 / 调整时长”等本地编辑动作，也不能在日视图拖动改时间。
- [ ] 暂停 / 启用 / 删除 OAuth 日历账号后，日历聚合结果按状态刷新。

## 9. 时区切换
- [ ] 设置几个闹钟提醒（H=20, M=30）。
- [ ] 把系统时区从 Asia/Shanghai 切到 America/New_York → 回到 App → 日志显示 `ReminderScheduler.resyncAll` 被触发；下次 20:30（新时区本地）仍触发。
- [ ] 壁钟时间不变，UTC 绝对时刻变。

## 10. 视觉态
- [ ] 加载中：LoadingState shimmer 条带正常动画。
- [ ] 错误态：ErrorState 显示错误文案 + "重试"按钮。
- [ ] 空态：EmptyState 含图标 + "新建"主按钮。

## 11. CloudSync v2
- [ ] `FeatureFlags.cloudSyncV2 = false`（默认）→ 登录、启动和本地数据变更都不发出同步请求。
- [ ] 手动覆盖 `cloudSyncV2 = true` + 登录 → 冷启动后自动同步成功，"有未同步改动"角标消失。
- [ ] 再修改一条任务 → 标记待同步并由后台队列自动同步；我的页不出现"立即同步"入口。

## 12. Empty Surface Audit
- [ ] 运行 `pwsh scripts/empty_surface_scan.ps1` → `docs/empty-surface-audit-raw.md` 生成，命中条目数不高于上次基线。
- [ ] `docs/empty-surface-audit.md` 中 ⏳ 的条目是否已关闭（准备发版前）。

## 13. 测试套件
- [ ] `flutter test test/` 全绿。
- [ ] `flutter test test/services/no_toast_reminder_static_test.dart` 强约束全绿（P15）。
- [ ] `flutter test integration_test/app_alignment_smoke_test.dart` 在真机或 emulator 上全绿。

## 14. 黑屏 / 崩溃
- [ ] 逐页打开：Today / Todo / Habit / Calendar / Pomodoro / Mine → 没有 FlutterError 红屏。
- [ ] 冷启动、切后台 30s、杀进程再启动，三轮不崩。

## 14a. 管理员用户状态
- [ ] 管理员后台 → 用户：每个用户显示“最近登录”和“最近活跃”，无记录时分别显示“从未登录 / 从未活跃”。
- [ ] 普通用户登录后，管理员用户列表显示在线；退出登录或超过在线窗口后，刷新列表显示离线。
- [ ] 概览页“7 日活跃”按最近登录或最近活跃统计，不把 8 天前无活跃用户计入。

---

## v2 升级新增项（任务 T-01 至 T-52）

### 15. 节假日数据（T-01 / T-44）
- [ ] 创建一个目标 → 设置每日重复 + 跳节假日 → 设置开始日期为 2026 春节前一天 → 下一派发日应跳过 2026-02-17 至 2026-02-23。
- [ ] 单元测试 `flutter test test/services/holiday_calendar_test.dart` 全绿。

### 16. 待办/目标提醒直连（T-02 / T-03 / T-45）
- [ ] 顺延一个已过期待办 → 通知队列同步更新（adb logcat 中无 `dynamic` 兜底日志）。
- [ ] 修改一个 active 目标的提醒时间 → `pending_notifications` 中条目同步更新。
- [ ] 单元测试 `flutter test test/services/reminder_scheduler_integration_test.dart` 全绿。

### 17. 目标下一派发日（T-04）
- [ ] Goal 编辑页"下一派发日"使用 RecurrenceEngine 计算 → 显示带节假日跳过的正确日期。

### 18. 纪念日强提醒（T-05 / T-30）
- [ ] 创建一个生日纪念日 → 编辑页切换"强提醒"开关 → 保存。
- [ ] 等待到提醒时间点 → 强提醒以全屏 intent 弹出（不仅是通知栏）。

### 19. 日历枢纽（T-07 / T-08 / T-09 / T-10）
- [ ] 日历日视图点击任一事件 → 弹出统一详情面板，根据类型显示对应操作（完成/打卡/开始专注/改期/编辑/跳转详情/删除）。
- [ ] 日历中点到番茄钟历史会话 → 进入统一详情面板后点"编辑" → 修改任务/日期/时长/白噪音/标签/自习室 → 保存后番茄钟历史和时间足迹同步更新。
- [ ] 在面板中"完成"一个待办 → 待办列表里该条目状态同步更新。
- [ ] 在面板中"改期" → 选择新日期 → 原模块日期已更新。
- [ ] 顶部 chips：点击"待办" → 仅显示待办；再点 → 取消；点"全部" → 恢复。
- [ ] 同时段创建两条待办 → 两条事件都显示冲突标记 ⚠️。

### 20. 单条提醒失败隔离（T-14）
- [ ] 故意写入一条非法 goals 数据触发 syncGoals 异常 → 其它 sync（todos/habits/anniversaries）仍正常运行（adb logcat 见 `[resyncReminders] syncGoals failed:` 单行后无中断）。

### 21. 时间足迹自动记录（T-18 / T-19 / T-20 / T-21 / T-47）
- [ ] 完成一个 25min 专注番茄 → 时间足迹页新增 1 条 focus 条目。
- [ ] 启用某待办的"专注链接"+ 时长 → 完成它 → 时间足迹页新增 1 条 todo 条目。
- [ ] 统计页底部出现"本周时间分布"图表，按来源饼图 + 按天柱状图渲染正常。
- [ ] 日历页 FAB → 点击 → 出现菜单 → 选"记录一段时间" → 输入事项、开始时间、分类、时长、备注 → 时间足迹页新增，分类和备注正确显示。

### 22. 共享协作（T-22 / T-23 / T-24 / T-25 / T-48）
- [ ] 我的 → 共享空间 → 点击"+" → 创建新空间 → 列表新增。
- [ ] 进入空间 → 点击"生成邀请码" → 选角色（editor / viewer）→ 复制邀请码。
- [ ] 在另一账号点"加入空间" → 输入邀请码 → 列表中出现该空间。
- [ ] 创建一个共享待办 → 待办卡片显示共享徽章（👥 图标）。
- [ ] 以 viewer 身份打开共享待办详情 → 编辑/删除按钮置灰或不可点。
- [ ] 以 owner/editor 身份在目标页创建共享目标 → 目标卡片显示共享空间标识；另一账号同步后可看到该目标。
- [ ] 以 viewer 身份打开共享目标编辑页 → 保存被拦截或编辑入口不可用，不能覆盖 owner/editor 的目标内容。
- [ ] 在日历页切到共享空间后新建本地日程 → 月/周/日/三日/年视图仅在该空间筛选下显示；切回 private 后不混入。
- [ ] owner/editor 修改共享本地日程后同步到另一账号；viewer 尝试编辑同一日程时被权限拦截。

### 23. 成就解锁与同步（T-26 / T-27 / T-28）
- [ ] 完成第 1/10/100 个待办 → 底部依次弹出成就解锁提示。
- [ ] 在新设备登录后同步 → 已解锁成就保持解锁状态。

### 24. 倒数日 / 课程深链（T-29 / T-31 / T-33）
- [ ] 设置一个 1 分钟后到期的倒数日 → 收到通知 → 点击 → 跳到倒数日页面。
- [ ] 课程表里有今日课程 → 日历日视图能看到课程事件 → 点击 → 弹详情面板 → "跳转详情"进入课程表。
- [ ] 桌面小组件中的"今日待办"点击其中一条 → 直接进入该待办详情。

### 25. 小组件完成动作（T-32 / T-34）
- [ ] 桌面小组件显示今日 Top3 待办，每条右侧有 ✓ 按钮。
- [ ] 点击 ✓ → App 内待办即时打勾 → 小组件刷新后该条消失/变化。
- [ ] iOS 工程接入 `ios/DuoyiWidgets` Extension target 后，10 个 WidgetKit kind（今日待办/专注/习惯/月历/今日日程/目标/课程表/随手记/纪念日/日记）都能在系统小组件列表单独添加，且不出现概览/组合入口。
- [ ] iOS Runner 与 Widget Extension 使用同一个 `group.com.duoyi.duoyi` App Group；App 内数据变化后，小组件读取到同一组待办、习惯、日程、目标、课程、随手记、纪念日和日记摘要。
- [ ] iOS 今日待办小组件点“+ 添加”进入快速待办；点待办标题进入待办详情，点“完成”走完成流程；专注小组件点“开始”启动番茄；习惯小组件点“打卡”对当前正向习惯打卡；底部待办/习惯/日历/专注导航分别进入对应模块。

### 26. 体验优化（T-35 / T-37 / T-38）
- [ ] 任一空数据页面（待办空 / 习惯空 / 目标空）→ 视觉一致（图标 + 文案 + 主操作按钮）。
- [ ] 全局搜索 → 输入"专注"等关键词 → 命中包括时间足迹条目。
- [ ] 导出备份 → 包含 `duoyi_time_entries` 与 `duoyi_achievements_unlocked` → 在另一台机导入后两类数据完整恢复。

### 27. 智能日期解析（T-39）
- [ ] 快捷新建任务 → 输入"明天下午3点开会" → 日期/时间字段被自动填入。
- [ ] 单元测试 `flutter test test/core/smart_date_parser_test.dart` 全绿。

### 28. Kanban 视图（T-40）
- [ ] 待办页右上视图切换图标 → 在列表 / 四象限看板间切换正常。

### 29. 专注与习惯增强（T-41 / T-42）
- [ ] 番茄页可输入标签 → 完成后会话写入 `tag` → 统计可看到标签分布。
- [ ] 习惯模型支持 `weeklyTarget` 字段 → 设置 5次/周后周视图显示 N/5。

### 30. 周报 / 月报（T-43）
- [ ] 我的 → 周报/月报入口 → 显示本周完成率、专注分钟、时间足迹总量、各类别分布。
- [ ] 单元测试 `flutter test test/core/report_engine_test.dart` 全绿。

### 31. 稍后提醒（T-12）
- [ ] 通过深链 `duoyi://snooze/{id}?delay=5` 模拟点击稍后提醒 → 5 分钟后再次收到通知；确认通知 action 与 deep-link 路径一致。

### 32. 文档体系（M0）
- [ ] 阅读 `docs/competitive-analysis.md` / `docs/requirement-v2.md` / `docs/design-v2.md` / `docs/task-v2.md` 内容连贯、链接互通。

---

**发版闸**：以上任何 ❌ 都不允许发稳定版。ℹ️ 项可以进待跟踪 Issue。
